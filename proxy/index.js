const UserProxy = require('./ins/user')
const TopicProxy = require('./ins/topic')
const ReplyProxy = require('./ins/reply')

module.exports = {
    UserProxy: UserProxy,
    TopicProxy: TopicProxy,
    ReplyProxy: ReplyProxy
}
