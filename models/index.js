const UserModel = require('./ins/user')
const TopicModel = require('./ins/topic')
const ReplyModel = require('./ins/reply')

module.exports = {
    UserModel: UserModel,
    TopicModel: TopicModel,
    ReplyModel: ReplyModel,
}